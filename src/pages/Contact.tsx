import { MapPin, MessageCircle, Phone } from "lucide-react";
import { clinic } from "../data/clinic";
import { BookingForm } from "../components/BookingForm";
import { SEO } from "../components/SEO";
import { formatPhone } from "../lib/utils";

function Hours() { return <div className="rounded-2xl border border-line bg-white p-6 shadow-card"><h2 className="font-display text-2xl font-semibold text-navy-900">Clinic hours</h2><dl className="mt-5 divide-y divide-line"><div className="flex justify-between gap-5 py-3 text-sm"><dt className="font-semibold text-navy-900">Monday–Saturday</dt><dd className="text-right text-muted">8:00 AM–1:00 PM<br/>4:30 PM–9:00 PM</dd></div><div className="flex justify-between gap-5 py-3 text-sm"><dt className="font-semibold text-navy-900">Sunday</dt><dd className="text-right text-muted">8:30 AM–1:00 PM</dd></div></dl></div>; }
export default function Contact() { const maps = encodeURIComponent(clinic.address.mapsQuery); return <><SEO title="Contact Rachana Physiotherapy Clinic | Manish Nagar, Nagpur" description="Call, WhatsApp or get directions to Rachana Physiotherapy Clinic in Manish Nagar, Nagpur. Request an appointment online."/><section className="section border-b border-line"><div className="container-site"><p className="eyebrow">CONTACT</p><h1 className="display mt-3 max-w-3xl text-[clamp(2.5rem,5vw,4.25rem)]">Let’s talk about <em>what feels difficult.</em></h1><p className="mt-5 max-w-2xl text-justify text-lg text-muted">Call, message or request an appointment. We’ll help you find a suitable next step.</p></div></section><section className="section"><div className="container-site grid gap-10 lg:grid-cols-[.95fr_1.05fr]"><div className="space-y-5"><div className="rounded-2xl bg-navy-900 p-7 text-white"><p className="text-xs font-semibold uppercase tracking-[.14em] text-gold-500">SPEAK WITH THE CLINIC</p><div className="mt-6 grid gap-3 sm:grid-cols-3"><a data-track="call" href={`tel:${formatPhone(clinic.phones[0])}`} className="flex min-h-14 items-center justify-center gap-2 rounded-lg bg-white px-3 text-sm font-semibold text-navy-900"><Phone size={17}/>Call</a><a data-track="whatsapp" href={`https://wa.me/${clinic.whatsappNumber}`} className="flex min-h-14 items-center justify-center gap-2 rounded-lg bg-whatsapp px-3 text-sm font-semibold text-white"><MessageCircle size={17}/>WhatsApp</a><a data-track="directions" href={`https://www.google.com/maps/dir/?api=1&destination=${maps}`} className="flex min-h-14 items-center justify-center gap-2 rounded-lg border border-white/40 px-3 text-sm font-semibold text-white"><MapPin size={17}/>Directions</a></div><p className="mt-5 text-sm text-white/70">{clinic.phones.join(" · ")}</p></div><Hours/><div className="rounded-2xl border border-line bg-sand p-6"><h2 className="font-display text-2xl font-semibold text-navy-900">Find us</h2><address className="mt-4 not-italic text-justify text-[15px] leading-relaxed text-muted">{clinic.address.line}</address><p className="mt-4 text-sm text-muted"><span className="font-semibold text-navy-900">Landmark:</span> {clinic.address.landmark}</p></div><div className="overflow-hidden rounded-2xl border border-line"><iframe title="Map to Rachana Physiotherapy Clinic" src={`https://www.google.com/maps?q=${maps}&output=embed`} loading="lazy" className="h-[310px] w-full border-0"/></div></div><div><BookingForm/><aside className="mt-8 border-t border-line pt-6"><h2 className="font-display text-2xl font-semibold text-navy-900">What to bring</h2><ul className="mt-4 list-disc space-y-2 pl-5 text-[15px] text-muted"><li>Previous reports, if you have them</li><li>X-ray or MRI images / reports, if relevant</li><li>Comfortable clothing that lets you move easily</li></ul></aside></div></div></section></>; }
